spring.application.name=basic-apache-camel

# By default the XML DSL route in src/main/resources/camel-routes/routes.xml is active.
# Activate the Java DSL route instead with: --spring.profiles.active=java-dsl

camel.springboot.routes-include-pattern=classpath:camel-routes/*.xml

logging.level.org.apache.camel=INFO
